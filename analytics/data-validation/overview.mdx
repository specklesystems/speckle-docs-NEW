---
title: 'Data Validation'
sidebarTitle: 'Overview'
description: 'Use Validation Checks and Standards to validate models and track results over time.'
---

Data Validation helps teams define quality rules once, run them consistently,
and catch issues before they become downstream coordination problems.

<Info>
  This page covers **Data Validation (checks and standards)** in the project Data
  Validation area. If you are using **dashboard-based validation widgets** such as
  Property Checker or Model Validation, use [Validation Widgets in Dashboards
  docs](/analytics/dashboards/validation-widgets).
</Info>

## Navigate from project sidebar

From an open project, use the left sidebar:

- Open **Data Validation**
- Use **Run check** to start a new check
- Use **Checks** to browse and open saved checks
- Use **Project standards** to create/import reusable standards

## Progressive implementation sequence

Use this three-step sequence to implement Data Validation in your project:

1. **Explore quickly**  
   Run an ephemeral check to test rule logic and understand how checks behave on
   real model data. Start at [Run a first
   check](/analytics/data-validation/checks#run-a-first-check).
2. **Persist useful checks**  
   Save checks against models so results become an ongoing quality signal, not a
   one-off review. Continue with [Operate checks over
   time](/analytics/data-validation/checks#operate-checks-over-time) and
   [Practical review flow](/analytics/data-validation/viewing-results#practical-review-flow).
3. **Scale with standards**  
   Store reusable standards and run them across multiple models in a single
   check to align teams at project scale. Use [Standards to checks
   flow](/analytics/data-validation/standards#standards-to-checks-flow).

This sequence is the practical path from experimentation to durable quality
governance.

## Start here

Choose the page that matches what you need to do next:

<CardGroup cols={2}>
  <Card
    title="Run and manage checks"
    icon="play"
    href="/analytics/data-validation/checks"
  >
    Define checks, run them against project models, and review check-level behavior.
  </Card>
  <Card
    title="Create project standards"
    icon="file-text"
    href="/analytics/data-validation/standards"
  >
    Build reusable project standards and apply them across checks.
  </Card>
  <Card
    title="Interpret results"
    icon="chart-column"
    href="/analytics/data-validation/viewing-results"
  >
    Read aggregate and model-level outcomes, then drill into rule logic and trends.
  </Card>
  <Card
    title="Using dashboard widgets instead?"
    icon="layout-grid"
    href="/analytics/dashboards/validation-widgets"
  >
    Follow the widget-based validation path in Intelligence dashboards.
  </Card>
</CardGroup>

## Why teams adopt it

Use Data Validation when quality expectations must be explicit, repeatable, and
visible across a project. Teams typically adopt it to reduce manual QA, align
disciplines around shared rule definitions, and track quality drift over time.

## Core objects

- **Validation Check**: execution object that runs rules against selected models
- **Standard**: reusable rule set template that can seed new checks
- **Result**: outcome for a run (score, statuses, affected objects)
- **History**: timeline of run outcomes for a check and tracked models

## End-to-end flow

<Steps>
  <Step title="Choose mode">
    Open Data Validation in a project and choose whether you are creating a **check** or
    a **standard**.
  </Step>
  <Step title="Author or import rules">
    Define check logic with `WHERE` and `CHECK`, or start from standards imported from
    **IDS**, **COBie**, or **Speckle** formats.
  </Step>
  <Step title="Run preview">
    Validate your rule logic before saving so you can adjust filters and thresholds.
  </Step>
  <Step title="Save">
    Save as a check (runs over tracked models) or as a standard (reused across checks).
  </Step>
  <Step title="Track results">
    Review latest result and history trends for each check, then investigate
    fails/warnings.
  </Step>
  <Step title="Share outcomes">
    If needed, export BCF for downstream issue coordination workflows.
  </Step>
</Steps>

## Plan availability

<AccordionGroup>
  <Accordion title="What Model Validation (beta) features do I get on each plan?">
    **Speckle Explore (Free):** in-session check and preview experience; no
    persistent saved checks and no saved results history.

    **Speckle Team (Business):** persistent checks with trend/history tracking
    for one tracked model per check.

    **Speckle Enterprise:** multi-model checks, reusable standards, and
    governance-oriented history at broader team scale.

  </Accordion>
</AccordionGroup>

## FAQ

<AccordionGroup>
  <Accordion title="Should I start with a check or a standard?">
    Start with a **check** when you are validating one concrete requirement against
    active models. Start with a **standard** when you want reusable rules that multiple
    checks can inherit.
  </Accordion>
  <Accordion title="Do checks run automatically?">
    Checks can run in manual mode or auto-run on new model versions, depending on check
    trigger settings.
  </Accordion>
  <Accordion title="Where do I inspect detailed outcomes?">
    Open the check detail page to inspect latest and historical rule outcomes. See
    [Viewing Results](/analytics/data-validation/viewing-results).
  </Accordion>
  <Accordion title="Can I still use dashboard widgets for validation?">
    Yes. Widget-first validation is documented in [Validation Widgets in
    Dashboards](/analytics/dashboards/validation-widgets).
  </Accordion>
</AccordionGroup>
