---
title: Navisworks - Manual Installation
description: "Manual installation instructions for Speckle Navisworks connector via ZIP"
---

<Info>
  **Install separately for each version**: Repeat these steps for each application version you want to use with Speckle.
</Info>

Before you begin, make sure Speckle Desktop Services is running in the background. All Speckle connectors require this service to function properly. [Learn more about Desktop Services](/connectors/manual-installation/desktop-services).

## Install for a single user

1. Extract the downloaded ZIP file
2. Copy `PackageContents.xml` from `.\Speckle.Connectors.Navisworks{VERSION}\win-x64\Plugin\` to `%AppData%\Autodesk\ApplicationPlugins\Speckle.Connectors.Navisworks.bundle\`
   - Replace `{VERSION}` with your Navisworks version (for example, `2026`)
   - Create the directory structure if it doesn't exist
3. Copy all contents from `.\Speckle.Connectors.Navisworks{VERSION}\win-x64\` to `%AppData%\Autodesk\ApplicationPlugins\Speckle.Connectors.Navisworks.bundle\Contents\{VERSION}\`
4. Copy all files that start with `NavisworksRibbon.` from `.\Speckle.Connectors.Navisworks{VERSION}\win-x64\Plugin\` to `%AppData%\Autodesk\ApplicationPlugins\Speckle.Connectors.Navisworks.bundle\Contents\{VERSION}\en-US\`

## Install for all users

Follow the same steps as the single user install, but use `%ProgramData%` instead of `%AppData%` in all file paths.

## After installation

**Disable update notifications**: To prevent the connector from checking for updates, [follow these steps](TODO).

## Uninstall the connector

To remove the Navisworks connector, delete the `Speckle.Connectors.Navisworks.bundle` folder from the location where you installed it.

To completely remove all Speckle products from your system, you may also need to delete the `%AppData%\Speckle\` folder.

<Card
  title="Back to Manual Installation Overview"
  icon="arrow-left"
  href="/connectors/manual-installation/manual-installation"
>
  Return to connector selection
</Card>
