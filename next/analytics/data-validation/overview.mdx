---
title: Data Validation in v2026.9
sidebarTitle: Data Validation
description: What changes for Data Validation after models move to the v2026.9 data format, including related objects and value mapping.
noindex: true
contextual:
  options: []
---

import NextPreviewBanner from '/snippets/next-preview-banner.mdx'

<NextPreviewBanner />

Use this page when your project is on the v2026.9 data format. Authoring, saving, standards, and
result review still work as in the current product. For those flows, see
[Data Validation](/analytics/data-validation/overview).

This page covers only the differences that change how a check scopes objects, reads properties,
compares an object to something it is related to, and maps labels before a comparison.

## What stays the same

Checks, standards, thresholds, plan caps, BCF export, and result history follow the current
guides:

- [Checks](/analytics/data-validation/checks)
- [Standards](/analytics/data-validation/standards)
- [Viewing Results](/analytics/data-validation/viewing-results)

Authoring still uses a scope and a check. In v2026.9 the editor shows those as short sentences.
A rule can also include a related object.

## Default object scope

In the current product, a new check starts with a global filter that keeps connector BIM objects
and drops proxy and geometry noise. In v2026.9, a new check starts with an **empty** global
scope. The version already contains user-facing objects only, so that starting filter is not
needed.

Do not follow the current [Default scope](/analytics/data-validation/checks#default-scope)
guidance on a v2026.9 version. Leave the global scope empty, then narrow with category, type, or
parameter filters in `WHERE` when you need a tighter rule.

<Warning>
  An existing check can return no objects on a v2026.9 version. Preview shows empty counts and an
  empty object table. The stored rule still uses the previous-format object filter. That filter is
  not shown as a control in the v2026.9 check editor, so you cannot clear it in place. Rebuild the
  check: create a new check, run preview, then save.
</Warning>

Unsaved drafts that still carry the old default are cleared when you reopen the editor. Saved
checks keep their stored rules. After save, you can edit name and description only.

## Checks against related objects

In the current product, a rule looks at properties on one object. In v2026.9 you can also check
that object against something it is related to, because those relationships are in the published
version.

Use **Add related object** on the rule. Pick a relationship such as **Host element**, **Room**,
or **Connected element**. Then choose a property on that related object from the path picker.

Examples you can author:

- A door's fire rating must match the wall that hosts it
- If the host wall is fire-rated, the door must be rated. Unrated walls are not applicable, not a
  fail
- Every door must be hosted on a wall
- Every wall that bounds a room carries a rating, or at least one of them does
- Doors can be checked against a room reached through the host wall (one related object chained
  from another)

**Add condition** adds a scope filter above the check. It does not turn the existing check into a
filter.

Rules display as short sentences, for example **category is Doors**. Select a sentence to edit
it.

<Note>
  **Add related object** is disabled when the preview version has no relationship data. Publish a
  new version with a current connector after the project uses the v2026.9 data format.
  Related-object rules then show that there is no data, rather than failing the whole run.
</Note>

## Map labels before you compare

When a check compares two properties (**Equals property**), v2026.9 can map each side's raw
labels onto shared values before it compares them. This is how `60 MIN` and `1 HR` become the
same rating, and how a `2 HR` wall can require a door of at least `90 MIN`.

Select **Equals property**, pick the other path, then open the comparison control beside it. The
control shows `=` until you add a mapping.

Set **Compare** to **equals**, **at least**, or **at most**. Under **This value maps** and
**Other value maps**, select **Add mapping**, pick a raw value from the list, and set what it
**maps to**. Unmapped values stay as they are.

Map both sides to the same numbers when the labels should count as equal. Map the other side to a
required minimum and use **at least** when one side must be stricter than the other.

The same mapping works when the other path is on a related object, such as a host wall.

<Tip>
  If preview already lists known values, **Seed N known values** fills both mapping lists so you do
  not type every label.
</Tip>

## Match a property wherever it sits

A Fire Rating parameter is not always on the same path. Click the path-matching control so the
condition uses the end of the path, for example every property whose path ends with **Fire
Rating**. Every matching value must pass. That covers instance and type placements without a
separate rule for each path.

## Property paths

Always pick property paths from the path picker after a preview model loads. Paths in the current
[Checks](/analytics/data-validation/checks#property-paths) examples, such as
`parameters.Width.value` and `baseline.length`, come from the previous data format and may not
exist on a v2026.9 version.

Typical Revit parameters now sit under `properties.Parameters` (for example type Width under
`properties.Parameters.Type Parameters.Dimensions.Width`). Use the picker for the paths that
exist on your model.

## Versions you can run against

Data Validation preview and saved-check runs need a version in the v2026.9 data format. If preview
does not load, publish a new version after the project has moved to v2026.9. See
[Speckle's data model is changing](/workspaces/data-model-migration).

## FAQ

<AccordionGroup>
  <Accordion title="Why does my existing check show 0 objects after the project moves to v2026.9?">
    The saved rule still uses the previous-format object filter. That filter is not shown in the
    v2026.9 check editor, and saved checks cannot change rules in place. Create a new check, run
    preview, then save. See [Default object scope](#default-object-scope).
  </Accordion>
  <Accordion title="Should I still drop DataObject to widen scope?">
    No. That step is for the current product. In v2026.9 the object set is already the user-facing
    elements. An empty global scope is the correct starting point. Narrow with category, type, or
    parameter filters in `WHERE` when you need a tighter rule.
  </Accordion>
  <Accordion title="Why is Add related object disabled?">
    The preview version has no relationship data. Publish a new version with a current connector
    after the project uses the v2026.9 data format. See [Checks against related
    objects](#checks-against-related-objects).
  </Accordion>
  <Accordion title="How do I check a door against the wall that hosts it?">
    On the door rule, select **Add related object**, choose **Host element**, then compare the
    door's fire rating to the host's fire rating. Map labels if one side uses minutes and the other
    uses hours. See [Compare ratings that use different
    words](#compare-ratings-that-use-different-words).
  </Accordion>
  <Accordion title="Do I rewrite every property path in every check?">
    Only when a path no longer exists on the new version. Load preview, pick the replacement from
    the path picker, and save a new check. Saved checks cannot change rules in place.
  </Accordion>
</AccordionGroup>
