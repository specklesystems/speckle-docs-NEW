# Files allowed to contain images or videos outside a <Frame>.
#
# Used by scripts/check-frames.mjs. One file path per line, `#` starts a
# comment. Exemptions are file-level so they do not churn when a page is
# edited.
#
# Legitimate reasons: inline icons, badges, logos, or decorative images where
# a framed border would look wrong. Screenshots and diagrams should be framed.
#
# `legacy/` is skipped entirely by the script — frozen historical content.
#
# This list is currently empty: every image and video outside legacy/ sits
# inside a <Frame>. Keep it that way if you can — add an entry only when a
# framed border is genuinely wrong for the image, not to silence the check.
