[tools]
node = "22.17.1"
pnpm = "10.30.3"

[tasks.install]
description = "Install dependencies"
run = "pnpm install"

[tasks.dev]
description = "Run the Mintlify dev server (port 3333)"
run = "pnpm run dev"

[tasks.validate]
description = "Validate the docs (mint validate)"
run = "pnpm run valid"

[tasks.check-links]
description = "Check for broken links and anchors"
run = "pnpm run check-links"

[tasks.uac-generate]
description = "Regenerate universal-ai-config outputs"
# Skills are pruned first: uac never removes outputs whose template disappeared
# (atlas/uac/README.md). Scoped to skills so hand-authored files survive.
run = "pnpm run uac clean --type skills && pnpm run uac generate"
